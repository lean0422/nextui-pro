import * as React from 'react';

interface {{ pascalCase name }}Props {
  children?: React.ReactNode;
}

export function {{ pascalCase name }}({ children }: {{ pascalCase name }}Props): JSX.Element {
  return (
    <div>
      {{ name }}
      {children}
    </div>
  );
};
